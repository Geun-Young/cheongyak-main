# 청약순위계산기 — 공고 자동 수집·자동 게시 설계서

작성일 2026-09-06 · 대상 저장소 `chan-hee1102/cheongyak` · 상태: 설계(코드 변경 없음)

> 이 문서는 "관리자가 매일 들어오지 않아도 공고가 실시간으로 들어오고, 깔끔하게 자동으로 목록에 올라가게" 만드는 설계다.
> 진행 기록(PROGRESS)에 적힌 현재 구조(공고 `announcements` + 유닛 `supply_units`, 조건은 jsonb, RLS는 `published`만 공개, 마이홈포털 수집 모듈)를 전제로 한다.

---

## 0. 시작 전 확인할 것 (막히는 지점)

진행 기록에 적힌 Supabase 연동·수집 코드가 **바탕화면 `cheongyak` 폴더와 GitHub main 어디에도 없다.** 둘 다 랜딩 커밋 `6e05b63`에 멈춰 있다. 그 작업이 든 사본을 찾아 먼저 푸시해야 이 설계를 얹을 수 있다. 다른 PC에서 했다면 그쪽에서 `git push`, 같은 PC라면 다른 폴더를 찾아 이 저장소로 옮긴다.

---

## 1. 문제 정의

지금 흐름은 이렇다.

```
API 수집 → status=draft, review_status=pending → (RLS가 숨김) → 관리자가 열어서 조건 입력 → published → 사용자에게 보임
```

병목은 "관리자가 열어야 보인다"는 한 줄이다. 공고 129건이 전부 숨겨져 있고, 관리자가 안 들어오면 사용자는 아무것도 못 본다.

**수집이 문제가 아니라 정책이 문제다.** 조건이 없는 공고도 UI는 이미 「확인 필요」 상태로 그릴 수 있다. 그런데 수집 단계에서 `draft`로 넣어 버려서 그 상태를 쓰지 못하고 있다.

## 2. 목표와 원칙

| 목표 | 기준 |
|---|---|
| 관리자 개입 없이 목록 갱신 | 하루 3회 자동 수집, 새 공고는 수집 직후 사용자에게 보임 |
| 보이는 것과 판정되는 것을 분리 | 공개 여부는 자동, 판정 가능 여부는 관리자 검수로 |
| 관리자 작업을 절대 덮어쓰지 않음 | 재수집이 요약·조건·순위 규칙을 건드리지 않음 |
| 조건 없이도 깔끔 | 자동 요약, 자동 마감, 중복 병합, 유형·지역 정규화 |
| 실패해도 조용히 망가지지 않음 | 수집 실행 로그, 이상 감지, 안전장치 |

원칙 한 줄: **소스가 주는 사실은 기계가, 해석(조건·요약·순위)은 사람이. 둘은 같은 행의 다른 칸에 산다.**

---

## 3. 상태 모델 재설계

### 3-1. 지금

- `status`: `draft | published | closed`
- `review_status`: `ready | pending`
- 수집분은 전부 `draft + pending` → 숨김

### 3-2. 제안

| 구분 | 값 | 누가 바꾸나 | 의미 |
|---|---|---|---|
| `status` | `published` | **수집기(기본값)** | 사용자에게 보임 |
| | `closed` | 수집기(자동) | 접수 마감일 지남. 「마감」 탭에만 |
| | `hidden` | 관리자 | 잘못된 데이터·중복·테스트. 사용자에게 안 보임 |
| | `draft` | 관리자 | 관리자가 손으로 만드는 중인 공고에만 |
| `review_status` | `pending` | 수집기(기본값) | 조건 없음 → 화면에 「확인 필요」 |
| | `ready` | 관리자 | 조건 입력 완료 → 판정 동작 |
| | `recheck` | 수집기(자동) | ready였는데 소스 값(접수기간·세대수 등)이 바뀜 → 관리자 재확인 큐 |

핵심 변경은 딱 하나다. **수집기가 `published + pending`으로 넣는다.** 관리자 검수는 "공개 게이트"에서 "판정 스위치"로 역할이 바뀐다.

RLS 정책은 `status = 'published'`를 `status in ('published','closed')`로 넓힌다(마감 공고는 목록의 「마감」 탭에서 보이므로). `hidden`·`draft`는 계속 숨긴다.

### 3-3. 사용자 화면에서의 표현

| 상태 조합 | 목록 배지 | 판정 열 | 상세 상단 |
|---|---|---|---|
| published + ready | 신청 가능 / 조건 미달 | 순위·가점 / 미달 항목 | 기존과 같음 |
| published + pending | **확인 필요** | "조건 정리 중 · 원문 확인" | "조건을 정리하고 있어요. 접수기간·임대료는 확인됐어요." + 원문 버튼 강조 |
| published + recheck | 신청 가능 / 미달 (기존 판정 유지) | 기존 + 작은 「변경됨」 표시 | "공고 내용이 바뀌어 다시 확인 중이에요" |
| closed | 접수 마감 | "접수가 끝났어요" | 기존과 같음 |

「확인 필요」 공고에 대해 사용자가 할 수 있는 행동을 하나 준다: **"조건 정리되면 알려주세요"** 버튼(회원) → 관리자가 ready로 바꾸는 순간 그 사용자에게 판정 결과 알림. 이게 관리자에게도 우선순위 신호가 된다(요청 많은 공고부터 검수).

---

## 4. 데이터 모델 변경 (마이그레이션 0002)

### 4-1. 소스 소유 칸과 관리자 소유 칸을 나눈다

```
announcements
  -- 소스 소유 (수집기가 매번 덮어씀)
  id                 text pk        -- 'myhome-{pblancId}' 형식 권장 (소스 접두어)
  source             text           -- 'myhome' | 'applyhome' | 'manual'
  source_id          text           -- 원본 공고번호
  title, agency_code, agency_name, region, district
  apply_start, apply_end, announced_at
  units              int            -- 유닛 합산
  original_url, original_url_kind
  housing_type_src   text           -- 소스가 준 유형 원문 (매핑 전)
  source_hash        text           -- 소스 필드 해시 (변경 감지)
  first_seen_at, last_seen_at, source_updated_at  timestamptz

  -- 관리자 소유 (수집기는 절대 건드리지 않음)
  status             text           -- 단, 'closed' 전환만 수집기가 함
  review_status      text           -- 단, 'recheck' 전환만 수집기가 함
  housing_type       text           -- 관리자가 고치면 유지. null이면 매핑값 표시
  summary            text[]         -- 관리자 요약. null이면 자동 요약 표시
  admin_note         text
  reviewed_at, reviewed_by

  -- 파생/캐시
  auto_summary       text[]         -- 규칙 기반 자동 요약 (수집기가 씀)
  request_count      int default 0  -- "조건 정리되면 알려주세요" 누른 수

supply_units
  id, announcement_id, seq
  -- 소스 소유
  name, area_m2, unit_count, deposit, monthly_rent, address, lat, lng, source_hash, last_seen_at
  active             boolean        -- 소스에서 사라지면 false (삭제 안 함)
  -- 관리자 소유
  eligibility, tiers, score_rules   jsonb
  housing_type, ranking_method, summary, move_in, rent_note

ingest_runs
  id, source, started_at, finished_at, status ('ok'|'partial'|'failed')
  fetched, inserted, updated, unchanged, closed, deactivated_units, errors jsonb
```

### 4-2. 왜 이렇게 나누나

- upsert를 **소스 칸만 명시한 컬럼 목록**으로 하면 관리자 칸은 구조적으로 못 건드린다. "조심해서 코딩"이 아니라 "칸이 달라서 못 덮어쓴다"가 된다.
- `housing_type`이 두 개인 이유: 마이홈 API의 유형(영구임대·50년임대·통합공공임대 등)을 우리 9개 유형으로 뭉뚱그려 넣고 있는데, 관리자가 고친 값을 다음 수집이 되돌리면 안 된다. 원문은 `housing_type_src`에 남기고, 표시값은 `coalesce(housing_type, 매핑(housing_type_src))`.
- `summary`/`auto_summary` 분리도 같은 이유. 화면은 `summary ?? auto_summary`.

---

## 5. 수집기 동작 규격

### 5-1. 실행 순서 (한 번의 run)

```
1. ingest_runs에 started 기록 (동시 실행 방지: 진행 중 run이 10분 이내면 종료)
2. 소스 호출 (마이홈 rsdtRcritNtcList + ltRsdtRcritNtcList, 페이지 끝까지)
3. 정규화: 기관·유형·지역 매핑, 제목 정리, 유닛 그룹핑, 자동 요약 생성, 해시 계산
4. 안전장치: 이번 응답 건수가 직전 run 대비 50% 미만이면 → 6번(마감 처리) 건너뛰고 'partial'로 종료 + 경고
5. upsert
   - 새 id → insert: status=published, review_status=pending, first_seen_at=now
   - 기존 id, 해시 같음 → last_seen_at만 갱신
   - 기존 id, 해시 다름 → 소스 칸 갱신, source_updated_at=now
       · review_status가 ready였으면 → recheck
       · 접수기간이 바뀐 경우 관리자 큐 상단으로
   - 유닛: 같은 규칙. 소스에서 사라진 유닛은 active=false
6. 자동 마감: apply_end < 오늘(KST) 이고 status=published → closed
7. 오래된 마감 정리: closed 후 30일 지나면 목록 쿼리에서 제외 (행은 보존)
8. ingest_runs에 finished + 집계 기록
```

### 5-2. 정규화 규칙 ("깔끔하게"의 실체)

| 항목 | 규칙 |
|---|---|
| 제목 | 앞뒤 공백·전각 괄호·연속 공백 정리, 기관명이 제목 맨 앞에 중복되면 제거 |
| 기관 | 소스의 `suplyInsttNm` → 코드 매핑표. 매핑 실패 시 `PRIVATE`가 아니라 **`UNKNOWN`으로 두고 관리자 큐** |
| 유형 | 매핑표를 9개에서 확장 권장: `통합공공임대`, `영구임대`, `50년임대`를 별도 값으로 둔다. 사용자에게 "국민임대"라고 보여줬는데 실제는 영구임대면 신뢰가 깨진다 |
| 지역 | `brtcNm` → 17개 시도. 실패 시 `전국`이 아니라 `null` + 큐 (전국으로 넣으면 모든 사람에게 보여서 오히려 지저분해진다) |
| 유닛(매입임대) | `houseSn`이 전부 0인 경우: 면적·보증금·월세가 같은 행끼리 묶어 하나의 유닛으로 합치고 `unit_count` 합산. 배열 순번 id는 재수집 때 순서가 바뀌면 깨지므로, 그룹 키 = `hash(면적,보증금,월세,주소)` |
| 중복(소스 간) | 마이홈 ↔ 청약홈 겹침은 `기관코드 + 접수시작일 + 제목 유사도(자모 정규화 후 0.85↑)`로 병합 후보 표시. 자동 병합은 하지 않고 관리자 큐에 "중복 의심" |

### 5-3. 자동 요약 (LLM 없이, 결정적)

관리자 요약이 없을 때만 표시. 소스 필드로 3~4문장을 조립한다.

```
[1] {기관명}이(가) {지역} {세부지역}에 공급하는 {유형} {세대수}세대예요.
[2] 접수는 {접수시작 M월 D일}부터 {접수마감 M월 D일}까지예요.            ← 마감 임박(≤3일)이면 "마감이 N일 남았어요" 덧붙임
[3] {유닛 요약}: "36㎡ 120세대, 46㎡ 80세대 두 타입이에요." / 보증금·월세 범위
[4] 자격 조건은 아직 정리 중이에요. 원문 공고문에서 소득·자산 기준을 확인하세요.
```

말투는 기존 요약과 같은 "~예요"로 맞춘다. 값이 없으면 그 문장은 뺀다(빈 칸 노출 금지).

### 5-4. 자동 마감과 노출 기간

- `apply_end < 오늘` → `closed`. 시간대는 **KST 기준 자정**으로 고정(서버 UTC로 두면 9시간 일찍 마감된다).
- 마감 후 30일까지는 「마감」 탭에 남긴다(사용자가 "그 공고 어떻게 됐지" 확인용). 이후 목록 쿼리에서 제외, 행은 보존(나중에 경쟁률·커트라인 데이터와 합칠 때 필요).
- 접수 시작 전 공고는 그대로 공개한다. 목록은 이미 "접수 시작까지 N일"을 그린다.

---

## 6. 스케줄러

### 6-1. 지금 당장 (배포 전): GitHub Actions cron

배포 여부와 무관하게 돌아가고, 저장소가 이미 GitHub에 있어서 가장 빠르다.

```yaml
# .github/workflows/ingest.yml
name: ingest-announcements
on:
  schedule:
    - cron: "10 23 * * *"   # KST 08:10
    - cron: "10 4 * * *"    # KST 13:10
    - cron: "10 9 * * *"    # KST 18:10
  workflow_dispatch:        # 수동 실행 버튼
concurrency: ingest         # 겹침 방지
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      - run: npx tsx scripts/ingest-myhome.ts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          DATA_GO_KR_KEY: ${{ secrets.DATA_GO_KR_KEY }}
```

- 저장소 Settings → Secrets에 세 값 등록. 서비스 롤 키는 여기와 서버에만 존재해야 한다.
- GitHub cron은 최대 수십 분 지연될 수 있다. 공고 데이터에는 문제없다.
- 실패 시 GitHub가 저장소 소유자에게 이메일을 보낸다. 별도 알림 없이도 최소한의 감시가 된다.

### 6-2. 배포 후: Vercel Cron 또는 Supabase pg_cron

- Vercel: `vercel.json`에 crons 등록 → `/api/cron/ingest` 라우트. 라우트는 `Authorization: Bearer ${CRON_SECRET}` 검증 필수. 수집 로직은 `src/lib/ingest/*`에 두고 스크립트와 라우트가 같은 함수를 부른다.
- Supabase pg_cron + pg_net: DB 안에서 스케줄 → 우리 라우트 호출. Vercel 무료 플랜의 cron 제한(하루 1회)에 걸리면 이쪽.

어느 쪽이든 **수집 함수는 하나**, 진입점만 둘이다.

---

## 7. 관리자 화면: 게이트에서 큐로

관리자가 "매일 들어와서 열어야 하는 문"이 아니라 "시간 날 때 처리하는 대기열"이 되도록 바꾼다.

| 요소 | 설계 |
|---|---|
| 기본 정렬 | `request_count` 내림차순 → 마감 임박 순. 사람들이 기다리는 공고부터 |
| 필터 탭 | 조건 없음(pending) / 재확인(recheck) / 매핑 실패(기관·지역 null) / 중복 의심 / 완료(ready) |
| 행 단위 액션 | 숨기기, 유형 고치기, **조건 템플릿 적용**(국민임대·행복주택 청년·매입임대·전세임대 템플릿으로 자격요건 80%를 채우고 수치만 확인), 원문 열기 |
| 다중 유닛 편집 | 유닛 탭 + "첫 유닛 조건을 나머지에 복사" 버튼. 마이홈 데이터 58건이 다중 유닛이라 이게 없으면 검수가 막힌다 |
| 완료 처리 | ready로 바꾸는 순간: 대기 신청자에게 판정 알림, 회원 전체 대상 "새 신청 가능 공고" 알림 후보로 등록 |
| 대시보드 | 최근 run 상태(시각·건수·오류), pending 건수, 오래 방치된 pending(7일↑) 경고 |

PDF→LLM 조건 초안은 이 큐의 "조건 템플릿 적용" 자리에 나중에 끼워 넣는다. 지금은 템플릿만으로도 검수 시간이 크게 준다.

---

## 8. 알림과 신선도 표시

- 회원 알림 규칙은 **ready인 공고에만** "신청 가능" 알림을 보낸다. pending 공고로 "신청 가능"이라고 보내면 안 된다.
- pending 새 공고는 선택형 알림 "관심 지역에 새 공고가 올라왔어요(조건 확인 중)"로 분리. 기본값은 끔.
- 목록 상단에 "마지막 갱신 N분 전" 한 줄. `ingest_runs`의 마지막 성공 시각을 쓴다. 자동화가 살아 있다는 걸 사용자에게 보여주는 가장 싼 방법이다.

---

## 9. 안전장치와 관측

| 위험 | 대응 |
|---|---|
| API가 빈 응답(장애)을 주면 전부 마감 처리됨 | 직전 run 대비 50% 미만이면 마감 단계 건너뜀, `partial` 기록 |
| 두 run이 겹침 | Actions `concurrency` + `ingest_runs` 진행 중 잠금 |
| 재수집이 관리자 작업을 덮어씀 | 소스 칸/관리자 칸 분리 + 명시적 컬럼 upsert (4장) |
| 소스 값이 바뀌었는데 옛 조건으로 판정 | `recheck` 상태 + 관리자 큐 |
| 키 노출 | 서비스 롤 키는 Actions Secrets·서버 env만. `NEXT_PUBLIC_` 금지. 크론 라우트는 `CRON_SECRET` |
| 연속 실패를 아무도 모름 | 2회 연속 `failed`면 관리자 이메일(초기엔 GitHub 실패 메일로 대체) |

---

## 10. 확장성·보안 점검 (습관대로)

**확장성**
1. 가장 먼저 닿을 자원: 없음에 가깝다. 공고 수백 건 × 하루 3회 upsert는 Supabase Free의 어느 한도에도 멀다. 공공데이터 한도(마이홈 일 1,000회)도 페이지네이션 포함 하루 30회 미만.
2. 회원 100/1,000/10,000명: 수집 비용은 회원 수와 무관. 판정 서버 이전 시점(로드맵 5번)에 다시 본다.
3. 임계점: 소스가 3개(마이홈·청약홈·LH 결과문서)로 늘고 경쟁률 데이터까지 쌓이면 `ingest_runs`와 히스토리 테이블이 커진다. 그때 월 단위 파티션. 비용 0.

**보안**
- ✅ 인증: 크론 라우트 `CRON_SECRET`, 스크립트는 Secrets. 관리자 페이지는 여전히 게이트 없음 — **로드맵 4번(인증) 전까지 배포 금지**
- ✅ 권한: RLS `published/closed`만 공개, 쓰기는 service_role만
- ✅ 시크릿: 서비스 롤 키 클라이언트 번들 유입은 `server-only`로 차단 중
- ⚠️ 입력: 소스 문자열(제목·주소)을 그대로 렌더한다. React가 이스케이프하지만 `dangerouslySetInnerHTML` 금지 유지

---

## 11. 작업 단위와 순서

| 순서 | 작업 | 산출물 | 규모 |
|---|---|---|---|
| 0 | 사라진 Supabase 코드 찾아서 푸시 | main에 `src/lib/ingest`, `scripts`, `supabase/migrations` | 확인 작업 |
| 1 | 마이그레이션 0002 | 소스/관리자 칸 분리 컬럼, `ingest_runs`, `supply_units.active`, RLS 확장, `hidden` 상태 | SQL 1개 |
| 2 | 수집기 리팩터 | 명시적 컬럼 upsert, 해시 변경 감지, `recheck`, 유닛 그룹핑, 자동 요약, KST 자동 마감, 안전장치, run 로그 | `src/lib/ingest/*` |
| 3 | 스케줄 | `.github/workflows/ingest.yml` + Secrets 3개 | 파일 1개 |
| 4 | 사용자 UI | 「확인 필요」 상세 문구·원문 강조, `summary ?? auto_summary`, "마지막 갱신", 「마감」 탭 30일 규칙, "정리되면 알려주세요" 버튼 | 컴포넌트 4~5개 |
| 5 | 관리자 큐 | 정렬·필터·행 액션·조건 템플릿·다중 유닛 복사 | 페이지 2개 |
| 6 | (배포 후) | `/api/cron/ingest` + Vercel Cron | 라우트 1개 |

1~3까지 끝나면 목표("관리자 없이 매일 갱신")는 달성된다. 4는 그 상태를 사용자에게 어색하지 않게 보여주는 일, 5는 관리자 시간을 줄이는 일이다.

---

## 12. 결정이 필요한 항목

1. **자동 공개 정책**: 수집 즉시 `published + pending`으로 공개하는 데 동의하는가. (대안: 기관이 LH·SH·GH처럼 신뢰 목록에 있을 때만 자동 공개)
2. **마감 후 노출 기간**: 30일이 적당한가.
3. **유형 세분화**: `통합공공임대`, `영구임대`, `50년임대`를 별도 유형으로 추가할지, 지금처럼 국민임대로 합칠지.
4. **pending 공고 알림**: "조건 확인 중인 새 공고" 알림을 옵션으로 둘지, 아예 안 보낼지.
5. **매입임대 유닛 병합 기준**: 면적·보증금·월세가 같으면 하나로 묶는 규칙에 동의하는가.

이 다섯 개만 정해지면 1~3번 작업은 바로 시작할 수 있다.
